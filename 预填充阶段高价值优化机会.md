# 预填充阶段高价值优化机会

> 分支: v12 (派生自 master, master 已合并上游 b10208)
> 调研日期: 2026-08-01
> 调研范围: src/llama-context.cpp, src/llama-graph.*, src/llama-kv-cache.cpp, common/*, tools/server/*, ggml/src/ggml-cuda/*, ggml/src/ggml-backend.cpp

---

## 核心结论

**本地特性几乎不污染 prefill 热路径**: EOG suppression, runaway detection, context-shift, reasoning sampling, hidden self-check 全部在 decode 阶段的 `process_token` 或 sampler apply 中执行, prefill 中间 token 不计算 logits, 零开销。**TurboQuant 是唯一在 prefill 有实际 GPU 开销的本地特性**, 但其旋转矩阵创建是 context-shift 专用, WHT 是固有开销。

真正的高价值改进集中在 4 个层面: **CUDA FA 调度, n_ubatch 调优, 图复用, CUDA Graph for MoE**。

---

## P0 - 最高价值 (投入产出比最高)

### 1. TurboQuant KV cache 的 prefill FA 强制走 f16 转换, 未用内联反量化 MMA

**位置**:
- [fattn.cu:752-783](ggml/src/ggml-cuda/fattn.cu#L752-L783) - turbo MMA 门控仅 `Q->ne[1] <= 4` 即 decode 生效
- [fattn.cu:633-655](ggml/src/ggml-cuda/fattn.cu#L633-L655) - prefill 返回 `MMA_F16`
- [fattn-common.cuh:1374-1434](ggml/src/ggml-cuda/fattn-common.cuh#L1374-L1434) - `launch_fattn` 因 `need_f16_K=true` 先把整 K/V 反量化到 f16 临时 buffer

**问题**: MMA 内核已实现内联 turbo 反量化 ([fattn-mma-f16.cuh:516-614](ggml/src/ggml-cuda/fattn-mma-f16.cuh#L516-L614)), 但 prefill 调度门控把它挡在外面, 反而走 f16 全量转换路径, 丢弃已有能力 + 多 2x 临时显存。

**收益**: 高 (FA 阶段 15-30%, 长上下文更高)

**难度**: 中。用 `generate_cu_files.py` 补 ncols1=16/32/64 的 turbo 实例, 放宽 [fattn.cu:759](ggml/src/ggml-cuda/fattn.cu#L759) 门控。

**风险**: 低 (decode 路径已验证, KLD 0.008396)。注意 turbo 路径 nstages=0 无 cp.async 流水, 大 batch 需 benchmark。

---

### 2. FA 因果 mask 对纯因果单序列 prefill 仍物化整张 KQ mask

**位置**:
- [llama-graph.cpp:26-40](src/llama-graph.cpp#L26-L40) - 4D mask `[n_kv, n_tokens/n_stream, 1, n_stream]`
- [llama-graph.cpp:467-496](src/llama-graph.cpp#L467-L496) - host 端逐元素填充
- [llama-graph.cpp:45-60](src/llama-graph.cpp#L45-L60) - `can_reuse_kq_mask` 仅当 n_kv, n_tokens 都不变才复用

**问题**: prefill 每个 ubatch 的 `n_kv` 随前置 ubatch 累积增长, mask 无法复用, 整段 prefill 的 mask 填充是 O(n_prefill^2/2)。对 8K prefill, n_ubatch=512 约 3500 万次浮点写 + host->device 上传。纯因果单序列时这张 mask 完全是下三角, 本可由 FA 内核内置 causal 选项直接处理。

**收益**: 高 (大 prefill 省数十毫秒 CPU + 带宽)

**难度**: 中。需 FA 内核支持 "无 mask + causal flag" 路径, 或多序列场景保留显式 mask 分支。

**风险**: 中。continuous batching 多序列时仍需显式隔离 mask。

---

### 3. n_ubatch 自动调优偏保守 + prefill/decode 共用单一值

**位置**:
- [llama-context.cpp:297-345](src/llama-context.cpp#L297-L345) - auto-tune, `*6` 固定倍率, 32K+ ctx 强制压到 1024/2048
- [llama-context.cpp:393](src/llama-context.cpp#L393) - `cparams.n_ubatch` 单值
- [llama-kv-cache.cpp:976](src/llama-kv-cache.cpp#L976) - init_batch 用同一值切分

**问题**:
- (a) `*6` 中间张量估算对 TurboQuant (KV cache 更小) 偏保守, 导致 n_ubatch 偏小
- (b) n_ubatch 同时服务 prefill 与 decode, 两者最优值差异巨大 (decode 期望小, prefill 期望大), 共用一值进退两难
- (c) 大 ctx 无脑压到 1024 打折用户 `-ub` 显式覆盖

**收益**: 高 (prefill 吞吐对 n_ubatch 极敏感)

**难度**: 中。引入 `n_ubatch_prefill` 需贯穿 cparams, sched_reserve, init_batch, server 侧。

**风险**: 中。放大 ubatch 放大 compute buffer, 需评估 CUDA graph 兼容性。

---

## P1 - 高价值

### 4. 末尾 ubatch 击穿图复用

**位置**:
- [llama-graph.h:722-791](src/llama-graph.h#L722-L791) - `allow_reuse` 要求 n_tokens/n_seq_tokens/n_seqs/n_seqs_unq 全等
- [llama-context.cpp:1472-1506](src/llama-context.cpp#L1472-L1506) - 不复用则 reset+build+sched_alloc, 毫秒级

**问题**: prefill 长度非 n_ubatch 整数倍时, 末尾 ubatch 的 n_tokens 较小触发完整图重建 + 调度器重分配; 多 slot 混合时 n_seqs_unq 漂移几乎每个 ubatch 都不复用。

**收益**: 高 (每次图重建大模型毫秒级, 多 ubatch 累积显著)

**难度**: 中高。padding 方案需处理 KV 写入副作用; 放宽复用需 ggml 侧配合。

**风险**: 中。padding 若处理不当写入错误 KV。

---

### 5. CUDA Graph 在 MoE prefill 完全禁用

**位置**: [ggml-cuda.cu:2598-2611](ggml/src/ggml-cuda/ggml-cuda.cu#L2598-L2611) - `ggml_cuda_graph_check_compability`, 存在 `MUL_MAT_ID` + 量化/TQ 权重或 `node->ne[2] > mmvq_mmid_max` 时整体禁用

**问题**: 所有 MoE 模型 (DeepSeek-V2/V3/V4, Qwen3-MoE, MiniMax) prefill 完全无法用 CUDA Graph, 每个 op 都有 launch 开销。小 ubatch 时 launch 开销可达数毫秒。注释已标 `TODO: figure out a way to enable for larger batch sizes`。

**收益**: MoE 模型小 batch prefill 5-15%

**难度**: 高。需改 MMQ/MVQ 路径避免捕获期 stream 同步。

**风险**: 中。CUDA Graph 要求捕获期无主机同步。

---

### 6. sched_reserve 每次 decode 重建调度器

**位置**: [llama-context.cpp:713-840](src/llama-context.cpp#L713-L840) - 行 736 `sched.reset(ggml_backend_sched_new(...))` 销毁重建整个调度器, 3 次 graph_reserve

**问题**: 连续 batch 场景 (server 多请求 prefill) 每次 decode 都重建调度器, 1-5ms 开销, 且使 `can_reuse` 首次必然失败。

**收益**: 连续 batch 每次 decode 省 1-5ms

**难度**: 中。判断图参数是否变化, 仅变化时才重建。

**风险**: 中。错误跳过重建可能导致 buffer 不足。

---

### 7. server 跨 slot 打包不足

**位置**:
- [server-context.cpp:4243-4299](tools/server/server-context.cpp#L4243-L4299) - 单 slot 填到 n_batch
- [server-context.cpp:503-508](tools/server/server-context.cpp#L503-L508) - `can_batch_with` lora 全等硬门槛

**问题**: 大 prompt slot 独占整个 n_batch, 其余 slot 的 prefill/decode 无法加入, GPU 利用率在该次 decode 内只服务一个 slot。lora 全等硬门槛阻止不同 lora 的 slot 同批。

**收益**: 高 (多并发请求 GPU 利用率与等效吞吐)

**难度**: 中高。轮转装填需重构批构建循环; lora 分阶段同批需 decode 内动态切换 adapter。

**风险**: 中。lora 切换语义, checkpoint 边界需重新验证。

---

## P2 - 中价值

### 8. TurboQuant Q pad+cont+WHT 图节点融合

**位置**:
- [llama-graph.cpp:2727-2733](src/llama-graph.cpp#L2727-L2733)
- [llama-graph.cpp:3052-3058](src/llama-graph.cpp#L3052-L3058) - head_dim 非 128 对齐时 Q 在 WHT 前需 pad+cont+WHT 三节点

**收益**: 每层减 2 节点, 64+ 层模型图构建/调度省 5-10% (非 128 对齐模型)

**难度**: 中。改 `ggml_turbo_wht` CUDA 内核支持非对齐输入 + 内部补零。

**风险**: 低-中。

---

### 9. AMD/HIP turbo prefill 强制走 VEC (逐 query 串行)

**位置**: [fattn.cu:620-631](ggml/src/ggml-cuda/fattn.cu#L620-L631) - HIP 下量化 KV 强制 `VEC` 避免 f16 buffer OOM, 注释承认 "prefill is slower"

**收益**: AMD GPU turbo KV prefill 显著 (VEC 串行 query 是当前瓶颈)

**难度**: 中高。需确认内联反量化 MMA (机会 1) 在 HIP 上 shmem 占用可接受。

**风险**: 中。AMD 架构多样。

---

### 10. prepare() 对所有 ubatch 做 dry-run 分配再整体回滚

**位置**:
- [llama-kv-cache.cpp:1014-1070](src/llama-kv-cache.cpp#L1014-L1070) - dry-run + 回滚
- [llama-kv-cache.cpp:3079-3103](src/llama-kv-cache.cpp#L3079-L3103) - apply 再调一次 apply_ubatch

**问题**: apply_ubatch 被调用两次 (dry-run + 真实), dry-run 还伴 cell 拷贝回滚。

**收益**: 中 (长 prefill 累积可观)

**难度**: 中。重构 prepare/apply_ubatch 职责切分。

**风险**: 中。KV cell 状态机正确性。

---

## 低优先级 (快速项 / 覆盖性)

- **op_offload 阈值 batch>=32** 限制小 ubatch prefill 算子上迁 ([ggml-cuda.cu:5467](ggml/src/ggml-cuda/ggml-cuda.cu#L5467)) - 可按 n_ubatch 自适应
- **graph_get_cb 的后端强制绑定** 可能制造额外 split ([llama-context.cpp:2619-2643](src/llama-context.cpp#L2619-L2643), 已标 FIXME)
- **CUDA Graph warmup 对变长 prefill 不友好** ([ggml-cuda.cu:4211-4229](ggml/src/ggml-cuda/ggml-cuda.cu#L4211-L4229)) - 可按 ubatch 大小缓存多 graph
- **load-mode mlock-ram 冷启动收益未充分利用** ([llama-mmap.cpp:532-578](src/llama-mmap.cpp#L532-L578)) - Windows PrefetchVirtualMemory 异步未等待
- **group_norm 未与 mul 融合** ([llama-graph.cpp:1462-1467](src/llama-graph.cpp#L1462-L1467)) - 仅影响 group_norm 模型
- **TurboQuant 类型检查按层缓存** ([llama-graph.cpp](src/llama-graph.cpp) 多处重复 `k->type == TURBO3_0 || ...`) - 代码清晰度

---

## 本地特性 prefill 开销评估 (汇总)

| 特性 | prefill 开销 | 说明 |
|------|-------------|------|
| TurboQuant 旋转矩阵创建 | 零 | KV cache 初始化时一次性创建, 不在 prefill 路径 |
| TurboQuant WHT op | 固有开销 | 每层 2 次 (Q 前向 + KQV 逆向), 可融合 (机会 8) |
| TurboQuant MMF 条件检查 | 极小 | 每 mul_mat 1 次布尔比较, 可重排优化 |
| EOG suppression / logit_bias_eog | 零 | 仅末尾 token 采样阶段 |
| runaway detection / context-shift | 零 | 全部在 decode 的 process_token |
| reasoning sampling overrides | 零 | 仅采样阶段 |
| hidden self-check turn | 不在初始 prefill | 触发时增加 10-50 token 小 prefill |
| auto batch size tuning | 一次性计算 | 6x 倍率对 TurboQuant 偏保守 (机会 3) |

---

## 推荐实施顺序

1. **P0-1 (TurboQuant FA 内联反量化)** - 内核能力已就绪, 主要是调度门控放宽 + 实例补全, 影响所有 turbo KV cache 用户, 投入产出比最高
2. **P0-3 (n_ubatch 调优 + prefill/decode 分离)** - 与 P0-1 协同放大收益, 且对非 TurboQuant 用户也有效
3. **P0-2 (FA 因果 mask)** - 收益高, 定位清晰, 可独立验证
4. **P1-4 + P1-6 (图复用 + 调度器复用)** - 协同项, 连续 batch 场景收益叠加
5. **P1-5 (MoE CUDA Graph)** - 难度高但 MoE 模型覆盖面广
6. **P1-7 (server 跨 slot 打包)** - 多并发场景收益高

---

## 验证方法

所有改动遵循 `AGENTS.md`: 最小侵入, 复用现有基础设施, 避免新子系统。落地前用 `llama-bench` 对每个改动单独测 prefill tg128/tg256 吞吐, 控制变量 (同模型, 同 n_ctx, 同 -ub)。

涉及 ggml 内核改动 (机会 1 的 FA 内置 causal, 机会 8 的 turbo-wht 融合) 需在 ggml 侧评估。

---

## 关键文件索引

| 关注领域 | 文件路径 | 关键行号 |
|---------|---------|---------|
| prefill 主循环 | src/llama-context.cpp | 1838 (decode), 1985-2163 (prefill loop), 1458 (process_ubatch) |
| 图复用 | src/llama-graph.h | 722-791 (allow_reuse) |
| n_ubatch 调优 | src/llama-context.cpp | 253-345 (auto-tune), 393 (cparams) |
| FA mask 构建 | src/llama-graph.cpp | 26-60, 467-496 |
| KV cache 切分 | src/llama-kv-cache.cpp | 976 (init_batch), 1014-1070 (prepare), 1165-1395 (find_slot) |
| build_rope_shift | src/llama-kv-cache.cpp | 2262-2318 (仅 context shift) |
| FA 调度 | ggml/src/ggml-cuda/fattn.cu | 498-707 (kernel 选择), 744-798 (turbo 门控) |
| MMA 内联 turbo 反量化 | ggml/src/ggml-cuda/fattn-mma-f16.cuh | 482-614 (load tile), 740-802 (nstages=0) |
| CUDA Graph | ggml/src/ggml-cuda/ggml-cuda.cu | 2586 (check_compability), 4191 (graph_compute) |
| 调度器 | ggml/src/ggml-backend.cpp | 1023 (split_graph), 1550 (compute_splits) |
| 融合算子 (图构建) | src/llama-graph.cpp | 1453 (build_norm), 2441 (flash_attn_ext) |
| server 批处理 | tools/server/server-context.cpp | 3490 (update_slots), 4243 (prefill 装填) |
| load_mode | src/llama-model-loader.cpp | 547-549, 1334 (init_mappings) |
| TurboQuant WHT | ggml/src/ggml-cuda/turbo-wht.cu | 23-79 |
| TurboQuant WHT 调用 | src/llama-graph.cpp | 2458, 2536, 2733, 2862, 3059 |
